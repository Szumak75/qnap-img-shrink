# Minimal requirements without Pillow (for platforms without compilation support)
# Use when Pillow cannot be compiled - requires ImageMagick instead

certifi==2026.1.4
charset-normalizer==3.4.4
idna==3.11
jsktoolbox==1.2.2
pyyaml==6.0.3
requests==2.32.5
urllib3==2.6.3

# Note: Pillow is excluded - use ImageMagick-based Converter2 instead
# Install ImageMagick: apt-get install imagemagick (or opkg on QNAP)
